A list of exercises.

Monday:

* study `examples/gcc`; execute make process

* study `examples/cmake_simple/`; execute the cmake build process

* convert `examples/gcc` to a simple cmake project. Put the resulting file in a (local) git repository where you keep
  track of your exercises.
  
